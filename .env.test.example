# Copy to ".env.test" in this directory and fill in (file is gitignored).
# pytest loads ".env.test" automatically if python-dotenv is installed (pip install -e ".[dev]").
# Existing environment variables are not overwritten.

BITMESH_TEST_CONSUMER_KEY=
BITMESH_TEST_CONSUMER_SECRET=
